description: Id không trùng khớp với đơn hàng nào
content:
  application/json:
    schema:
      properties:
        message:
          title: Thông báo ngắn
          type: string
          description: Không có kết quả truy vấn nào cho mô hình [ App\Models\Order ] 1
          example: No query results for model [ App\Models\Order ] 1
      type: object
